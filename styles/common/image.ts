import { StyleSheet } from "react-native";

export const ImageStyles = StyleSheet.create({
    image: {
        position: 'absolute',
        top:-140,
        left: 0,
        right: 0,
        height: 320, 
        width: '110%',
        resizeMode: 'cover',
        zIndex: -2,
  
      }
})