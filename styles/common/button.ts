import { StyleSheet } from "react-native";

export const ButonStyles = StyleSheet.create({
    button: {
        backgroundColor: 'red',
        padding: 15,
        borderRadius: 20,
        alignItems: 'center',
        
      },
      buttonText: {
        color: '#fff',
        fontSize: 16,
      },
     
      
})